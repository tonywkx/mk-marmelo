export const renderTitle = (title) => {
  return (
    <h2
      className="font-greed font-medium text-[clamp(35px,5vw,55px)] leading-[1.01562] tracking-[-0.02em] text-dark-green mb-[clamp(20px,5vw,50px)]">
      {title}
    </h2>
  )
}
